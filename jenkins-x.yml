pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: "node:alpine"
        options:
          timeout:
            time: 1
            unit: 'HOURS'
               
           
