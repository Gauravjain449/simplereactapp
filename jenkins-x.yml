pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: "node:alpine"
        environment:
          - name: CI
          - value: "true"
        options:
          timeout:
            time: 1
            unit: "HOURS"
